import type { ThemeConfig } from '/domain/services'

/**
 * Theme config
 * @title Theme
 * @description The theme config for the engine
 * @default
 * {
 *   type: "tailwindcss"
 * }
 */
export type ThemeServiceSchema = ThemeConfig
